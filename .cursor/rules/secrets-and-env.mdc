---
description: 密钥与敏感文件保护
alwaysApply: true
---

# 密钥与敏感信息

## 禁止

- 不要读取、展示或提交 `.env` 内容
- 不要在代码、测试、日志中硬编码 API Key / Token
- 不要 `git add .env` 或任何含密钥的文件

## 允许

- 参考 `.env.example` 了解变量名与默认值
- 通过 `config.settings` 访问运行时配置

## 日志

- 打印配置时用字段名，不打印完整 URL 若含凭证
- 错误信息不泄露环境变量值
