# /test — 运行测试并报告

运行项目测试套件，修复失败项（仅限本次改动引入的问题）。

## 步骤

1. 在项目根目录执行 `make test`
2. 若失败，只修复与当前任务相关的测试或代码
3. 再次运行直到通过
4. 汇报：通过数、失败原因、是否改了测试

## 约束

- 不跳过测试（不用 `-k` 选择性忽略除非用户指定）
- 不引入需要 Ollama / Docker 的集成测试到默认套件
